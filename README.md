# FINAL-PROJECT

This is a general video game quiz that tests your knowledge on various games

Just install the file and run it in visual studio code.

It uses api fetching to fetch questions from an external api and uses a database to display a leaderboard
I would have added more questions and a streak multiplier if i had more time. I would have also tweaked the database.
